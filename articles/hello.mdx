# Hello, Next.js!

```js
console.log(Hello World)
export default function => () {
    rerere
}
```

 
You can import and use React components in MDX files.

## This should be a h2 element

export default ({ children }) => <div className='prose'>{children}</div>